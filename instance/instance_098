"""
从键盘输入一个字符串，将小写字母全部转换成大写字母，然后输出到一个磁盘文件"test"中保存

"""

fp = open('test', 'w')
string = input('input one string: ')

string = string.upper()

fp.write(string)

fp = open('test', 'r')
print(fp.read())

fp.close()
